# OpenAPI contract

The machine-readable API contract is maintained in
[`docs/openapi.yaml`](https://github.com/sayanmohsin/go-feather-route/blob/main/docs/openapi.yaml).

It describes the health, model discovery, and chat completion endpoints. Keep
the contract synchronized with handler behavior and validate it in CI when the
API changes.
